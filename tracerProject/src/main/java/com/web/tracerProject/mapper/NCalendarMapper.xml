<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.tracerProject.mapper.NDaoCalendar">
    <select id="getScheduleCalendarList" resultType="com.web.tracerProject.vo.Calendar">
        SELECT
			SID AS ID, TITLE, EMAIL AS WRITER, 
			TO_CHAR(START_DATE, 'YYYY-MM-DD') AS "START",
			TO_CHAR(END_DATE, 'YYYY-MM-DD') AS "END", 
			DESCRIPTION AS CONTENT,
			'#2ecc71' AS BACKGROUNDCOLOR,
			'WHITE' AS TEXTCOLOR, '#' AS URL,
			'true' AS ALLDAY
		FROM SCHEDULE
    </select>
    
    <insert id="insScheduleCalendar" parameterType="com.web.tracerProject.vo.Calendar">
        INSERT INTO SCHEDULE 
	  		(SID, TITLE, EMAIL, START_DATE, END_DATE, DESCRIPTION, PID)
	  		values('SID'||LPAD(SID_SEQ.NEXTVAL, 5, '0'),
	  		#{title},#{writer},#{start},#{end},
	  		#{content}, '')
    </insert>
    
    <update id="uptScheduleCalendar" parameterType="com.web.tracerProject.vo.Calendar">
        UPDATE SCHEDULE
		        SET title = #{title},
		            start_date = #{start},
		            end_date = #{end},
		            email = #{writer},
		            description = #{content}
		        WHERE sid = #{id}
    </update>
    
    <delete id="delScheduleCalendar" parameterType="String">
       DELETE FROM SCHEDULE WHERE sid = #{id}
    </delete>
    
    <select id="getScheduleCalendarListIndiv" parameterType="int" resultType="com.web.tracerProject.vo.Calendar">
        SELECT * FROM users WHERE id = #{id}
    </select>
    
    <insert id="insScheduleCalendarIndiv" parameterType="com.web.tracerProject.vo.Calendar">
        INSERT INTO users (name, email) VALUES (#{name}, #{email})
    </insert>
    
    <update id="uptScheduleCalendarIndiv" parameterType="com.web.tracerProject.vo.Calendar">
        UPDATE * FROM users WHERE id = #{id}
    </update>
    
    <delete id="delScheduleCalendarIndiv" parameterType="String">
        DELETE * FROM users WHERE id = #{id}
    </delete>
    
    <select id="getScheduleCalendarListGroup" parameterType="int" resultType="com.web.tracerProject.vo.Calendar">
        SELECT * FROM users WHERE id = #{id}
    </select>
    
    <insert id="insScheduleCalendarGroup" parameterType="com.web.tracerProject.vo.Calendar">
        INSERT * FROM users WHERE id = #{id}
    </insert>
    
    <update id="uptScheduleCalendarGroup" parameterType="com.web.tracerProject.vo.Calendar">
        UPDATE * FROM users WHERE id = #{id}
    </update>
    
    <delete id="delScheduleCalendarGroup" parameterType="String">
        DELETE * FROM users WHERE id = #{id}
    </delete>
</mapper>